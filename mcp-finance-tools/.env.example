# Optional: override port when running the server
# PORT=8000

# Required for get_stock_quote (Finnhub). Free key: https://finnhub.io/register
# Copy this file to .env and set your key (python-dotenv loads .env on startup).
FINNHUB_API_KEY=
